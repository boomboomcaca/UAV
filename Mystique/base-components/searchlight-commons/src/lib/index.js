import gradientColors from './color';
import { createGUID, getGUIDTime, createTimeID } from './random';
import { getTimePercent, getTimeSpan, getTimeStamp, getTimeSpanHMS } from './timeHelper';
import { solidData, getSysDate } from './tools';

export {
  gradientColors,
  createGUID,
  getGUIDTime,
  createTimeID,
  getTimePercent,
  getTimeSpan,
  getTimeStamp,
  getTimeSpanHMS,
  solidData,
  getSysDate,
};

import DataList from './DataList.jsx';

DataList.Type = {
  Single: 'single',
  Segments: 'segments',
  Segment: 'segment',
};

DataList.Func = {
  FFM: 'ffm',
  Scan: 'scan',
};

export default DataList;

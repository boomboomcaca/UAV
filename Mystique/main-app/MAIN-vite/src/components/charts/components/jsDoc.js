/**
 * @typedef {Object} Marker
 * @property {String} id
 * @property {Number} dataXIndex
 * @property {Number} dataYIndex
 * @property {{x:Number,y:Number}} position
 * @property {Boolean} selected
 * @property {Boolean} visible
 * @property {Boolean} inRain
 * @property {Boolean} mouseInCenter
 * @property {Boolean} hover
 */

/**
 * @typedef {Object} ZoomInfo
 * @property {Number} start
 * @property {Number} end
 * @property {Number} startIndex
 * @property {Number} endIndex
 * @property {Number} zoomLen
 */

/**
 * @typedef {Object} viewOptions
 * @property {Boolean} axisX
 * @property {Boolean} axisY
 * @property {Boolean} toolBar
 * viewOptions:{axisX:Boolean,axisY:Boolean,toolBar:Boolean}
 */

// start: zoomStartPct,
// end: zoomEndPct,
// startIndex: startIndex,
// endIndex: endIndex,
// zoomLen: zoomLen,
